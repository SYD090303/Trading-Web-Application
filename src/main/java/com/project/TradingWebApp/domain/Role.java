package com.project.TradingWebApp.domain;

/**
 * Enum representing the different roles a user can have in the system.
 * Roles define the access level and permissions for users.
 */
public enum Role {

    /** Standard user role with limited access. */
    USER,

    /** Administrator role with elevated permissions and full access. */
    ADMIN
}
